services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password

  adminer:
    image: adminer:latest
    ports:
      - "8083:8080" # Map ra port 8083 theo đúng yêu cầu đề bài
    depends_on:
      - db